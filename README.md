# x-autopilot-updates
Auto-update feed for [X-Autopilot](https://github.com/worklab-studio/x-autopilot) Mac app.

`appcast.xml` is the Sparkle-format feed the running app polls every 24h. DMG
binaries are attached as release assets — see the Releases tab.
